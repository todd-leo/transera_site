# ---- Python 基础 ----
__pycache__/
*.py[cod]
*$py.class
*.so

# 虚拟环境
.venv/
venv/
env/
ENV/
.pipenv/
pipenv/
poetry.lock
.poetry/
pip-wheel-metadata/

# 包管理 / 构建
*.egg-info/
.eggs/
dist/
build/
wheels/
*.egg

# 测试与覆盖率
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/
.dmypy.json
.pyre/
.pytype/

# 日志与临时
*.log
*.tmp
*.swp
*.swo
*.orig

# ---- Django / Wagtail ----
# SQLite 等本地数据库（若生产用 Postgres/MySQL，通常忽略本地 DB 文件）
db.sqlite3
db.sqlite3-journal

# 收集的静态文件（由 collectstatic 生成；如果你把编译后的静态直接提交仓库，请移除此项）
staticfiles/
static_root/
static-collected/

# 媒体上传（用户上传内容通常不进版本库）
media/
media_root/

# 迁移（一般是需要提交的！若你有特殊流程再改）
migrations/  # 不要默认忽略迁移
home/migrations/
merchants/migrations/
users/migrations/

# Wagtail 搜索索引（如使用本地 Whoosh/临时索引）
.whoosh/

# Wagtail/Django 运行产生的缓存
cache/
tmp/
django_cache/
*.pid

# ---- 前端（如用 Webpack/Parcel/Vite 等）----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/
.pnp.*
.parcel-cache/
.cache/
.dist/
coverage/
# 前端构建输出（按你的配置调整）
frontend/dist/
frontend/.cache/
static/build/
static/dist/

# ---- IDE / 编辑器 ----
# JetBrains
.idea/
*.iml

# VS Code
.vscode/
.history/

# macOS / Windows 杂项
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# ---- Docker / 容器 ----
.docker/
docker-compose.override.yml
# 卷挂载的持久化目录（按需）
data/
postgres-data/
redis-data/

# ---- 环境与配置（按需忽略）----
.env
.env.*
!.env.example
*.local
*.bak

# ---- Sphinx / 文档（如使用）----
docs/_build/